# ACC-20260804-02 — 驗收:接上 autopilot runner(CHG-20260804-02)

- 驗收者:Claude(Cowork session;與實作者同一個)
- 風險分級:低
- 分支:main
- 日期:2026-08-04(UTC+0)
- 對應變更:CHG-20260804-02

## 查核明細

| # | 驗收條件 | 結果 | 證據 |
|---|----------|------|------|
| 1 | runner 可執行 | 通過 | `tools/autopilot/scripts/autopilot_runner.py --help` exit 0 |
| 2 | 所有 plan 格式 CHG 通過 plan-check | 通過 | 7 張;非 plan 格式者由 CI 具名 skip 並印出 |
| 3 | 存量 CHG 補齊而非隱藏 | 通過 | CHG-20260804-01 補上 Global Constraints 與三個 task 的 interfaces/test,內容取自 ACC-20260804-01 已記錄的驗證方式 |
| 4 | 漂移檢查綠燈可達 | 通過 | 42 檔一致,來源 commit 記於 PROVENANCE |
| 5 | 漂移檢查**紅燈可達** | 通過 | 改動 `tools/autopilot/scripts/static_check.py` → exit 1;還原後回 0 |
| 6 | 同一支工具不再有兩份 | 通過 | 扁平 `tools/*.py` 已移除,只剩 `tools/autopilot/scripts/` 底下那一份 |
| 7 | 靜態檢查範圍明示且通過 | 通過 | `--paths skills plugins`;`tools/` 的四項 shell=True 由上游具名豁免涵蓋,完整性交給漂移檢查 |
| 8 | 其餘 CI 步驟仍綠 | 通過 | doc-integrity、static、雙語成對檢查、py_compile、JSON、副本同步、catalog |

## 這一輪最值得記的一件事

接上 plan-check 的**當下**,既有的 CHG-20260804-01 就紅了。

那是新閘對存量生效的標準形態,而它有兩條路:

- 把存量**補齊**——要花時間,但帳本從此對得起這道閘
- 把存量改成閘**看不見**的格式——把 `- [x] T1` 拆掉就會被當成非 plan 格式而略過

第二條省事、CI 一樣會綠、而且從結果上看不出來做過什麼。這正是這套治理最想擋的東西,
所以選第一條。補上去的內容都是 ACC-20260804-01 已記錄過的驗證方式,沒有事後編造。

## 還沒做到的

- 停點矩陣帶進來了,但這個 repo還沒有真的跑過一輪 `run`(plan → build → accept)。
  能跑不等於跑過——目前只驗到 `plan-check` 與 `--help`。
- 上游前進的偵測仍是盲點,見 PROVENANCE。

## 結論

**通過。** 這個 repo 從「被靜態檢查」變成「跑得動治理流程」,而且第一件事就是被自己接上的閘擋了一次。
