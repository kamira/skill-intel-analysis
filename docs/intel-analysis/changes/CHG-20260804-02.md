# CHG-20260804-02 — 接上 autopilot runner:從「被靜態檢查」升為「跑得動治理流程」

- Project: skill-intel-analysis / skill: intel-analysis
- Branch: main
- Date: 2026-08-04 (UTC+0)
- Type: 治理能力
- Proposed by: 使用者(「新的三份獨立 skill 由 ai-sdlc-autopilot 治理」)
- Implemented by: Claude(Cowork session)
- Risk: 低(只新增隨身副本與 CI 步驟,不動 skill 內容)
- Recurrence check: 非重複。首次把執行層帶進衛星 repo。

## 動機

拆分當下這個 repo 只帶了四支**檢查器**:doc-integrity、static、bilingual、knowledge-index。
那是「被檢查」,不是「被治理」——`plan-check` / `build` / `review` / `verify` / `accept`
與停點矩陣都不在,於是這裡的 CHG 格式沒有任何東西在把關,寫成什麼樣都不會紅。

## 為什麼帶整份 scripts/ 而不是算相依閉包

runner 會以**字串**指名同層腳本(`static_check.py`、`mutation_harness.py`、
`autopilot_sentinels.py`)。那種相依 import 分析看不到,算漏一支的後果是
**跑到那條路徑才炸**——而那通常正好是有人真的要用停點的時候。

另一個不可壓平的細節:`lib/` 以 `parent.parent.parent / "assets"` 定位資產,
所以目錄結構必須原樣保留為 `tools/autopilot/scripts/lib/` + `tools/autopilot/assets/`。

## 新閘對存量生效

接上 `plan-check` 之後,**既有的** CHG-20260804-01 立刻紅了:缺 `### Global Constraints`、
三個 task 都缺 `interfaces:` 與 `test:`。

正確處置是把存量**補齊**,不是把存量改成閘看不見的格式(把 `- [x] T1` 拆掉就會被當成
非 plan 格式而略過——那是最省事也最沒有意義的解法)。補上去的內容都是
ACC-20260804-01 已經記錄過的驗證方式,不是事後編的。

## 靜態檢查的範圍要明示

帶進來之後靜態檢查立刻紅四項 `shell=True`(`exec_util`、`ci_gate`、
`autopilot_sentinels`、`mutation_harness`)。上游**已經**對這四處具名豁免,
但豁免記的是上游路徑 `skills/ai-sdlc-autopilot/scripts/...`,對不上這裡的 `tools/autopilot/...`。

三條路:

| 做法 | 為什麼不選 / 選 |
|------|-----------------|
| 改帶進來的 allowlist | ✗ 那是就地改副本,漂移檢查會紅——而且該紅 |
| 在本 repo 另開一份 allowlist 重寫理由 | ✗ 同一件事兩個地方各有一份理由,遲早分岔 |
| **把靜態檢查範圍明示收在自己的程式碼** | ✓ 副本是逐位元組相同的東西,完整性由漂移檢查負責;重新稽核一份雜湊相符的副本不會增加資訊 |

代價寫明:repo **根目錄**新增的 `.py` 不在 `--paths skills plugins` 範圍內。
目前根目錄沒有 `.py`,若之後有,要一併加進範圍。

## 使用者故事

1. 作為維護者,我要這個 repo 跑得動 plan-check,以便 CHG 格式有東西在把關。
2. 作為維護者,我要停點矩陣在這裡也有效,以便不該自動化的動作在這裡一樣擋得住。
3. 作為維護者,我要隨身副本被改過會紅,以便就地分叉不會無聲發生。

## 修改指引

### Goal

把 autopilot 的執行層(runner + lib + assets)帶進本 repo 並接上 CI。

### Global Constraints(每個 task 都要遵守)

- 隨身副本**不得**就地改;要改就送回上游再同步下來
- 目錄結構原樣保留(`lib/` 以相對層級定位 assets)
- 不動任何 skill 內容

### Tasks(checkbox=續作點)

- [x] T1. 帶入 `tools/autopilot/`(scripts 不含測試 + lib + assets,共 41 檔)
  - interfaces: consumes skill-ai-sdlc-autopilot / produces tools/autopilot/
  - test: `autopilot_runner.py --help` 可執行;`tools_drift_check.py` exit 0
- [x] T2. 舊的扁平 `tools/*.py` 由 `tools/autopilot/scripts/` 底下同一份取代
  - interfaces: consumes 舊副本 / produces 單一來源
  - test: 同一支工具不再有兩份;PROVENANCE 涵蓋 42 檔
- [x] T3. 補齊 CHG-20260804-01 的 plan 欄位;CI 加上 plan-check 步驟
  - interfaces: consumes plan-check / produces 合格的存量帳本
  - test: 本 repo 所有 plan 格式 CHG 皆通過 plan-check
- [x] T4. 靜態檢查範圍明示收在 `skills plugins`,`tools/` 交給漂移檢查
  - interfaces: consumes static_check --paths / produces 不重複稽核副本
  - test: 靜態檢查通過;`tools/` 的四項 shell=True 由上游具名豁免涵蓋

## Acceptance operation(實際操作驗收)

- **operate**:(1) 跑 `autopilot_runner.py --help`;(2) 對本 repo 每一張 plan 格式 CHG 跑 plan-check;(3) 跑漂移檢查,並刻意改一支後重跑;(4) 跑完整套 CI 步驟。
- **observe**:各自的退出碼與訊息。
- **pass**:runner 可執行;所有 plan 格式 CHG 通過;漂移檢查綠燈與紅燈皆可達;CI 全綠。

## Risk & rollback

- 風險:隨身副本會與上游分岔 → 緩解:漂移檢查抓得出本地改動;上游前進查不出來,已於 PROVENANCE 明說為盲點
- 風險:帶了用不到的腳本 → 緩解:PROVENANCE 的 `_wired.entrypoints` 標明 CI 實際接線的五支,其餘是被指名的相依
- 回滾:刪除 `tools/autopilot/` 並還原扁平副本

## 狀態

已驗收(見 ../acceptance/ACC-20260804-02.md)。
