# CHG-20260707-02 — 每日盤點 runner(帳本側狀態機;資訊獲取由使用者自理)

- Project: ai-skills / skill: intel-analysis
- Branch: claude/quirky-margulis-cad156
- Date: 2026-07-07 (UTC+0)
- Type: 新功能(紀律 19〜23 的帳本側自動化)
- Proposed by: 使用者(「next」+ 範圍修正:「每日獲取資訊的部分不處理 那是用戶自己處理來源」)
- Implemented by: Claude(Cowork session, worktree quirky-margulis-cad156)
- Risk: 中(新增狀態機腳本與 coverage 記錄格式;不動既有 lint/帳本格式)
- Commit/PR: PR https://github.com/kamira/ai-skills/pull/7(與本變更同 commit;message 帶編號)
- 重複性檢查(Recurrence check): 與 autopilot CHG-20260706-01 動機重複(第 2 次「紀律 runner 化」)→ 更新 intel 庫 KN-001 方向一致;另候補假說:紀律類規則的自動化=「runner 出清單+人/agent 出判斷+帳本記結果」三段式
- Skill: ai-sdlc v1.17
- Related: intel CHG-20260707-01(帳本與 lint 基礎);disciplines.md 紀律 19-23;autopilot-loop(exit code 契約先例)

## 動機
紀律 19-23 的每日操作有一半是確定性機械活(盤點誰、誰到期、誰連續無訊號、記錄處置),散文執行必漏。**明確排除**:定向搜尋/lookback 抓取/命中比對的資訊面——來源與判讀由使用者自理;runner 永不呼叫任何搜尋或網路。

## 決策與取捨
| 決策 | 選項 | 前提假設 | 為何這樣選 |
|------|------|----------|------------|
| runner 只管帳本側,不碰資訊獲取 | 內建 agent-cmd 搜尋 | 使用者明定來源自理 | 邊界乾淨:runner=狀態機與記錄員,判斷與資料是使用者的 |
| coverage 記錄=每日一 JSON(append-only) | 寫回快照/單一累計檔 | 快照不可覆蓋;計數要跨日可稽核 | `docs/intel/coverage/YYYY-MM-DD.json` 一日一檔,冷鏈計數由讀最近 N 檔推得 |
| tracking_status 可就地更新於 latest | 一律新快照 | Notion 原機制即允許驗證時就地填 latest | 機率/內容變更才需新快照;追蹤狀態轉換(休眠/喚醒)屬營運欄位 |
| 休眠只「建議」不自動改檔 | runner 直接改 | 降冷鏈是判斷(紀律 21 允許喚醒例外) | runner 列候選+理由,使用者確認後自行改或叫 agent 改 |

## 修改指引

### 目標
`intel_daily.py` 三子指令(status/brief/log)落地;紀律 19(a,b,d 記錄面)、20(真空偵測)、21(冷鏈計數與候選)、23(視窗到期清單)機械化;紀律 19(c) 查證與 22 戰區掃描的**資訊面留給使用者**(簡報只列「該查什麼」,不代查)。

### Global Constraints(全域約束——每個 task 都要遵守)
- stdlib-only;**零網路呼叫、零搜尋**;不修改任何既有快照的機率/內容欄
- coverage 檔 append-only(當日檔已存在→合併新條目,不覆蓋既有 chain 條目)
- 不動既有 prediction_lint/brier_report 行為

### Tasks(checkbox=續作點)
- [x] T1. references/daily-loop.md(驅動契約:狀態機、A/B/C/D 處置、冷鏈計數規則、使用者/runner 分工邊界、exit code)
  - interfaces: consumes disciplines 19-23+prediction-ledger / produces 一檔
  - test: 明文含「資訊獲取由使用者自理」邊界句與四類處置表
- [x] T2. scripts/intel_daily.py(status/brief/log;coverage 檔格式;冷鏈=最近 3 個 coverage 檔皆 A)
  - interfaces: consumes 帳本 JSON+coverage 檔 / produces 單檔 runner
  - test: py_compile;<300 行
- [x] T3. SKILL.md 機器輔助節+偵測表加 daily-loop 列
  - interfaces: consumes T1-T2 / produces SKILL.md 編修
  - test: grep intel_daily 命中
- [x] T4. fixture 驗證:盤點清單正確(排除 verified/dormant)、視窗到期列出、log 寫入+append 合併、連續 3 日 A→冷鏈候選、brief 不含任何搜尋指令
  - interfaces: consumes T2 / produces 驗證輸出(入 ACC)
  - test: 全斷言過
- [x] T5. build_suite 重同步+plugin README/root README 一行提及 daily runner
  - interfaces: consumes T1-T3 / produces 複本+README 編修
  - test: --check exit 0
- [x] T6. ACC-20260707-02 收尾;Commit/PR 回填;commit+PR+merge
  - interfaces: consumes AC 證據 / produces ACC+PR
  - test: 欄位齊;PR merged

### 結構文件更新
- [ ] coverage 檔格式記於 daily-loop.md(即正典);Guideline 範圍句由本 CHG 動機節修正記載(原「每日情報 autopilot」縮為帳本側)

### 風險與回滾
- 風險:冷鏈誤判(緩解:只出候選不自動改);coverage 檔併寫衝突(緩解:一日一檔+合併寫)
- 回滾:revert 本 commit;coverage 目錄可整體刪除,不影響快照帳本

## 代決事項(confirm gate 記錄)
使用者已定邊界(資訊自理)。代決:coverage 路徑 `docs/intel/coverage/YYYY-MM-DD.json`;冷鏈閾值 3(紀律 21 原文);exit code 沿用 0/1/2 慣例(無合法停點語意,不用 3)。

## 審議判決(中風險;spawn 停滯→序列自審,註明限制)

```
[verdict] risk | claude-fable-5 | pass | 零網路+append-only+只建議不改檔 | 最壞情況=清單不準,無資料破壞面
[verdict] impact | claude-fable-5 | pass | 不動既有 lint/帳本 | 全增量;coverage 為新獨立目錄
[verdict] drift | claude-fable-5 | concern→resolved | tracking_status 就地更新 vs 快照不可覆蓋 | 明文區分:內容/機率=新快照,營運狀態欄=latest 就地(與 Notion 原機制一致,記入 daily-loop.md)
```

## 狀態
已驗收(見 ../acceptance/ACC-20260707-02.md)
