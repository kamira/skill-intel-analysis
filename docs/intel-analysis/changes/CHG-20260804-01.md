# CHG-20260804-01 — 從 ai-skills 拆出為獨立 repo(單一 plugin 的 marketplace)

- Project: skill-intel-analysis / skill: intel-analysis
- Branch: main
- Date: 2026-08-04 (UTC+0)
- Type: 結構(repo 拆分)
- Proposed by: 使用者(「將裡面的三個主要 skill plugin 拆出三個私人 repo,分開治理與編輯」)
- Implemented by: Claude(Cowork session)
- Risk: 低(skill 內容未動,只改載體與新增治理工具)
- Recurrence check: 非重複。首次把單一 plugin 拆成獨立 repo。

## 動機

`kamira/ai-skills` 是一個裝了三個 plugin 的 monorepo。使用者要把三者拆成三個私人 repo,
各自治理與編輯。本 repo 是其中之一。

## 這一輪的實質改動

skill 內容**一個字都沒動**,原封搬過來。改的只有載體,外加把治理能力帶進來:

| 項目 | 改法 |
|------|------|
| `.claude-plugin/marketplace.json` | 改為只列 `intel-analysis` 一個 plugin |
| `tools/` | 從 `kamira/skill-ai-sdlc-autopilot` 帶來四支治理工具的**副本** |
| `tools/PROVENANCE.json` + `tools_drift_check.py` | 記下帶過來當下的雜湊,並在 CI 檢查本地有沒有被改過 |
| `.github/workflows/governance.yml` | 新建:漂移、doc-integrity、靜態/安全、雙語成對檢查、py_compile、JSON、副本同步、catalog |
| `plugins/build_suite.py` | `PLUGINS` 只留自己那一組對映 |
| `AGENTS.md` / `README.md` | 重寫為單一 plugin 的進場說明 |

## 隨身工具的漂移是明說的盲點

`tools/` 是副本,不是本 repo 的原創。分開治理的代價就是副本會漂,而漂移有兩個方向:

- **本地被改過** —— `tools_drift_check.py` 查得出來(雜湊對不上)
- **上游前進了** —— 它**查不出來**,要主動同步

第二種是已知盲點,所以那支程式不印任何「工具是最新的」之類的話。
把查不出來的事說成查過了,比不查更糟。

## 使用者故事

1. 作為使用者,我要能對這個 repo 直接 `/plugin marketplace add`,以便不必安裝另外兩個不相干的 plugin。
2. 作為維護者,我要這個 repo 有自己的治理閘,以便拆出來之後不是「沒人管」。
3. 作為維護者,我要隨身工具被改過會紅,以便就地分叉不會無聲發生。

## 修改指引

### Global Constraints(每個 task 都要遵守)

- skill 內容**不得**有任何實質改動(只准改載體)
- 帳本整份帶走,不裁切
- 帶進來的治理工具是**副本**,不得就地改

### Tasks(checkbox=續作點)

- [x] T1. 組出 repo 樹(skill + plugin + docs)
  - interfaces: consumes ai-skills@45d7de3 / produces repo 樹
  - test: 檔案數與來源一致;`doc_integrity_check` 通過
- [x] T2. 帶入治理工具與漂移檢查
  - interfaces: consumes 上游 scripts / produces tools/ + PROVENANCE.json
  - test: `tools_drift_check.py` exit 0;改動任一支後 exit 1
- [x] T3. 建 CI 工作流並在本地重現每一個步驟
  - interfaces: consumes T1-T2 / produces governance.yml
  - test: 每一個 CI 步驟在本地逐項跑過

## Acceptance operation(實際操作驗收)

- **operate**:在拆出來的 repo 內逐項跑 CI 的每一個步驟。
- **observe**:各自的退出碼。
- **pass**:全綠;且刻意改動 `tools/` 內任一支後漂移檢查必須轉紅。

## Risk & rollback

- 風險:漂移檢查若清單為空會恆綠 → 緩解:空清單直接回 exit 2,且已實測改一個字元即轉紅
- 回滾:來源 `kamira/ai-skills@45d7de3` 完整保留

## 後補的欄位(CHG-20260804-02)

本張原本用的是簡寫的 Tasks 形式。接上 autopilot runner 之後 `plan-check` 對**存量**也生效,
於是補上 `### Global Constraints` 與每個 task 的 `interfaces:` / `test:`。

補的是**當時實際做過的事**,不是事後編出來的:三個 task 的 test 欄寫的都是
ACC-20260804-01 已記錄的驗證方式。新閘對存量生效時,正確處置是把存量補齊,
不是把存量改成閘看不見的格式。

## 狀態

已驗收(見 ../acceptance/ACC-20260804-01.md)。
